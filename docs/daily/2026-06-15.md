# 日報 2026-06-15

## 概要
06-13 日報の「次にやること」にあった Phase 6（実利用フィードバック対応）を `feat/phase6` ブランチで一通り実装し、実機確認まで完了した。あわせてポートフォリオ用 README の整備を行った。続けて実機利用で判明した新たなギャップを Phase 7 としてドキュメント化した。

## 実施したこと

### 1. ポートフォリオ向け README 整備
- たたき台 README を実装計画・コードベースと照合し、実態に合わせて全面改訂
- 誤記の修正（Ktor バックエンド → サーバーレス直呼び出し、Compose Multiplatform → Jetpack Compose）
- 実装済み機能一覧・技術スタック・データフロー図・ロードマップ（Phase 6 中心）を追記
- コミット: `d91d0a5`（`main`）

### 2. Phase 6 着手準備
- `feat/phase6` ブランチを `main` から作成（命名は従来の `feat/phase5` 等に合わせた）
- 着手済みの未コミット変更（6.1 ナビゲーション・6.5 通知アイコン）をブランチ上で2コミットに分割して記録

### 3. Phase 6.1 ナビゲーション修正（M1）
- `MainActivity` を `singleTask` 化し、ランチャー再起動時は常にカメラタブへ復帰
- ボトムタブ切替時に詳細・修正・プレビュー等の子画面を pop（`navigateToTabRoot` / `isTabRoute`）
- 通知ディープリンク（`EXTRA_RECEIPT_ID`）時のみ詳細へ遷移する挙動は維持
- コミット: `d23826e`

### 4. Phase 6.5 通知アイコン修正（M1）
- `ic_notification.xml`（レシート風シルエット）を追加
- `AnalysisNotifications` の `setSmallIcon` をアダプティブランチャーから切り替え（白丸解消）
- コミット: `335098e`

### 5. Phase 6.2 解析失敗の手動再送信（M2）
- `AnalysisWorker`: 1回失敗で即 `FAILED` 確定（WorkManager 自動リトライ廃止）、ユーザー向けエラー文言
- `ReceiptRepository.resendAnalysis()`: キュー再投入・ガード（APIキー未設定・画像欠落・処理中）
- レシート詳細・通知タブに「再送信」UI（確認ダイアログ・3秒クールダウン・設定誘導）
- 実機確認済み
- コミット: `c3732a9`, `b982ebd`

### 6. Phase 6.3 修正画面の本格化（M3）
- `ExpenseLineEditor` を `ui/common` に共通化（`ManualExpenseScreen` と修正画面で共有）
- `ReceiptReviewScreen` を手入力同等のフル編集に拡張
  - レシート画像の折りたたみプレビュー
  - 明細の追加・削除（確認ダイアログ）・全項目編集
  - 日付/時刻ピッカー、調整行の自動付与
- `applyReceiptEdit()` を新設し保存確定フローを統一
- 保存後は前画面（詳細等）へ `popBackStack`
- 実機確認済み
- コミット: `7579858`, `c18533f`

### 7. Phase 6.4 無駄遣い度（necessityScore）基準の明確化（M3 続き）
- `AnalysisWorker` プロンプト改訂: 商品用途軸の強調、店種別一括判定の禁止、境界ケース具体例
- `ReceiptJsonSchema`: `necessityScore` の description 追記
- 分析タブに「AI 目安・境界50・修正画面で調整可」の1行注記
- `docs/fixtures/necessity_score_regression.json` に回帰用期待スコア帯5件を記録
- コミット: `6ec6775`, `3afa6ce`
- ※プロンプト変更は新規解析分から反映。既存レシートは再送信しない限り変わらない

### 8. 実機利用で判明したギャップのドキュメント化（Phase 7 着手前）
Phase 6 完了後の実機確認で、以下3点を発見し、タスクとして明文化した（コード変更は未着手）。

| 課題 | 概要 |
|------|------|
| **初回オンボーディング** | チュートリアル・APIキー入力・権限要求が要件断片のみで、実装計画にタスクがなかった |
| **Drive バックアップ** | Google ログインは成功するが、バックアップ実行時にパーミッションエラー（403 等）で失敗 |
| **APIキー未設定時の送信** | プレビューで送信確定しても理由が表示されず、`AnalysisWorker` がサイレントスキップする |

実施内容:
- `docs/KNOWN_ISSUES.md` を新規作成（症状・想定原因・関連コード・対応案）
- `IMPLEMENTATION_PLAN_REVISED_2026-06-12.md` に **Phase 7**（7.1〜7.3）と推奨着手順を追記
- `REQUIREMENTS.md` に「実装ギャップ」セクションを追加
- `DEBUGGING_GUIDE.md` §8（Drive 403 切り分け）、`EXTERNAL_SETUP.md` §6（SHA-1 再登録手順）を追記
- Drive 403 は開発 PC 載せ替え後の **debug.keystore SHA-1 不一致**が有力候補（06-13 日報の注意と整合）

## 結果・所見

- **Phase 6（6.1〜6.5）は計画どおり完了**。06-13 時点の「次にやること」M1〜M3 をカバーした
- 1か月フィードバックの核（撮影体験の速さ）は維持しつつ、日常のストレス（ナビ・失敗時・修正・スコア基準）を順に解消できた
- `feat/phase6` は `main` より 9 コミット先行（README 1 + Phase 6 系 8）。未 push のまま
- necessityScore の改善は主観確認が必要。フィクスチャを素材に、今後の再解析でスコア帯を追跡する
- Phase 6 完了後も **初回体験・バックアップ・APIキー周り**に実用上の穴が残っている。Phase 7 で優先度付け済み（7.3 → 7.2 → 7.1）

## 変更ファイル（主なもの）

| 領域 | ファイル |
|------|----------|
| ドキュメント（README・Phase 6） | `README.md`, `docs/fixtures/necessity_score_regression.json` |
| ドキュメント（Phase 7 整理） | `docs/KNOWN_ISSUES.md`, `IMPLEMENTATION_PLAN_REVISED_2026-06-12.md`, `REQUIREMENTS.md`, `DEBUGGING_GUIDE.md`, `EXTERNAL_SETUP.md` |
| ナビ | `MainActivity.kt`, `Routes.kt`, `AndroidManifest.xml` |
| 通知 | `AnalysisNotifications.kt`, `res/drawable/ic_notification.xml` |
| 解析 | `AnalysisWorker.kt`, `ReceiptJsonSchema.kt`, `ReceiptRepository.kt`, `ReceiptDao.kt` |
| UI | `ReceiptDetailScreen.kt`, `NotificationsScreen.kt`, `ReceiptReviewScreen.kt`, `ExpenseLineEditor.kt`, `ManualExpenseScreen.kt`, `AnalysisScreen.kt` |

## 次にやること

- [ ] `feat/phase6` を push して PR 作成・`main` へマージ
- [ ] **Phase 7.3**: APIキー未設定時の送信ガード（プレビュー + Worker）
- [ ] **Phase 7.2**: Drive バックアップ 403 の切り分け（SHA-1 再登録・エラー表示改善）
- [ ] **Phase 7.1**: 初回オンボーディング（ウィザード・権限）
- [ ] Phase 5 残タスク: 解析状態の可視化統一（一覧 / 詳細 / 通知）
- [ ] Phase 5 残タスク: 通知履歴の永続化
- [ ] necessityScore 改訂後の実レシートでスコア帯を主観確認（フィクスチャと照合）
- [ ] `backups/dev/` の別ドライブ退避（06-13 からの任意タスク）

## メモ

- ブランチ: `feat/phase6`（`d23826e` 〜 `3afa6ce`）
- 実機確認済み: 6.2 再送信、6.3 修正画面。6.1 / 6.5 も着手時に確認想定
- 06-13 の `JAVA_HOME` / adb 設定は引き続き有効（Gradle ビルド・退避スクリプト用）
- Phase 7 ドキュメントは未コミットの可能性あり（日報・KNOWN_ISSUES 等）
