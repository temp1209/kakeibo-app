# Phase 9.5 — コンセプト・機能ブレスト

> **ステータス**: ブレスト中（方向性合意済み・詳細設計前）  
> **作成日**: 2026-07-13  
> **最終更新**: 2026-07-14  
> **作業ブランチ**: `feat/phase9.5-brainstorm`  
> **前提**: Phase 9（必須度ポリシー）完了後の方向性検討

---

## 1. アプリのコア（合意済み）

| 軸 | 内容 |
|----|------|
| **主目的** | **家計の可視化**（記録 → 見える化） |
| **副目的** | **節約**（裁量支出の把握・抑制の補助） |
| **コンセプト** | **記録がメイン、節約がサブ** |
| **入力** | レシート撮影が主。手入力は例外 |
| **AI の役割** | OCR・分類・必須度付与。ユーザーは方針と訂正例でチューニング |
| **スコープ** | 個人利用。複雑な家計簿機能にはしない |

### やらないこと（合意）

| 項目 | 理由 |
|------|------|
| **コーチ寄り機能** | 機械からのアドバイス・説教は好まない。数値と状態の提示に留める |
| **ジャンル別予算** | 自分の使い方に過度に合わせる必要はない。全体予算で十分 |
| **科目別の細かい予算管理** | 一般家計簿化。差別化薄い（E2 と同趣旨） |

### Phase 9 までで得たもの

- ユーザーごとの必須／裁量の「方針」
- 訂正例 → コンパイル → 解析・再スコアへの反映
- プリセット別のスコア帯・境界ケース

### まだ弱いところ（合意・優先度付き）

| 優先 | 課題 |
|------|------|
| **高** | **解析失敗体験** — 特に API 上限（429 / quota）。いま一番つらい |
| 中 | 月全体の予算に対する消費の見え方（通知含む） |
| 中 | レシートが増えたときの探す・振り返る |
| 低 | プロンプト改善の過去レシートへの反映（再解析など） |

---

## 2. Phase 9.5 スコープ案（たたき台）

### 柱 1: 解析失敗体験の改善（最優先）

**ゴール**: 失敗しても「何が起きたか・次に何すればいいか」がアプリ内で完結する。OS 通知に頼らない。

| 項目 | 内容 |
|------|------|
| 一覧・詳細での失敗表示 | 理由を `GeminiUserMessages` ベースで常時表示（レート制限は明示） |
| 再送信導線 | 失敗レシートからワンタップ再解析（既存手動再送信の強化） |
| 上限時の案内 | 「しばらく待つ」に加え、無料枠の性質・確認先（Google AI Studio 等）へのリンクは任意 |
| キュー状態 | 解析中 / 失敗 / 要確認を一覧で一目で（5.2 の延長） |
| 通知との分離 | 失敗は **アプリ内で十分**。解析完了 OS 通知は別途オフ可能に（柱 2） |

**スコープ外**: 自動リトライ（バックオフ Worker）は初版は見送り可。ユーザー操作の再送信を明確にする方が先。

### 柱 2: 通知の整理

| 通知種別 | デフォルト | 設定で個別 ON/OFF |
|----------|------------|-------------------|
| **解析完了** | OFF | ✅ |
| **解析失敗** | 権限許可時のみ ON（下記） | ✅ |
| **要確認（低信頼）** | OFF | ✅ |
| **月次予算**（10/20/月末 + 閾値） | OFF（予算有効時も初期 OFF） | ✅ |
| **すべての通知**（マスター） | ON（ただし種別ごと初期は上表） | ✅ |

#### オンボーディング（通知権限ステップ）

- ユーザーが **通知権限を許可したときだけ** → **解析失敗通知のみ ON** にする
- 許可しなかった／スキップした → 種別はすべて OFF（マスターは ON のままでも実質送信なし）
- 文言: 「解析完了」ではなく **失敗時のお知らせ** と予算ペース通知の説明に更新（完了通知は推さない）

#### 設定 UI（通知セクション）

```
[ すべての通知 ]  ── マスタートグル（OFF で個別トグル無効化 or 一括 OFF）

  解析が失敗したとき
  解析が完了したとき
  要確認のレシートがあるとき
  月次予算の進捗（10日・20日・月末）
```

- マスター OFF → OS 通知は一切出さない（Worker 側でもガード）
- マスター ON → 個別トグルに従う
- 予算機能無効時は「月次予算」トグルは非表示 or 無効

**実装メモ**: `NotificationPrefs`（DataStore or SharedPreferences）にマスター + 種別フラグ。既存ユーザーは移行時 **すべて OFF**（失敗のみ ON にしたい場合は設定で明示ON）

### 柱 3: 月次全体予算 + 消費ペース

**ゴール**: コーチではなく **鏡**。数字と割合だけ伝える。

| 項目 | 内容 |
|------|------|
| **オンボーディング** | **APIキー直後**に予算ステップを追加。紹介 + 月額入力を促す。**スキップ可** → **予算機能無効**。**新規のみ** |
| 月の全体予算 | 1つだけ（円） |
| **集計対象（設定）** | デフォルト: **支出合計（必須+裁量）**。切替: **裁量のみ** |
| 分析タブ表示 | **棒全体 = 月次予算**。その中に必須・裁量を **左から積み上げ**。未使用分は空き |
| 予算無効時 | 予算枠なしの **支出のみ積み上げ棒**（幅 = 必須+裁量）。設定で予算有効化を促す |
| **定期通知** | 月次リズムに合わせ **10日・20日・月末**（固定。個別カスタムはしない） |
| 閾値通知 | 例: 80% / 100% 到達時（ON/OFF 可） |

**集計**: `receipt_items` 調整行除外。対象は上記設定に従う。

**分析タブ UI（合意）**

**予算有効時** — 棒の全幅が月次予算。支出はその中に必須→裁量の順で積み上げる:

```
予算 ¥80,000
[■■必須 ¥45k■■][■裁量 ¥12k■][      残り ¥23k      ]
 ←──────────── 棒全体 = 予算 100% ────────────→

使用 ¥57,000（71%）　必須 32行 / 裁量 18行
```

- **棒全体の幅** = 月次予算（100%）
- **左から積み上げ**: 必須寄り（≥50）→ 裁量寄り（<50）。各セグメント幅 = その金額 ÷ 予算
- **右側の空き** = 予算残り（未使用）
- **予算超過時**: 棒は満杯表示 + テキストで超過額（例: `予算超過 ¥5,000`）。セグメントは予算内に収まるようクリップ or 超過分を別色で右端に示す（実装時に決定）
- 色: Material3 で必須 / 裁量 / 残り（surfaceVariant）を区別

**集計軸が「裁量のみ」のとき**

- 棒全体は依然 **予算**
- 積み上げは **裁量分のみ**（必須は棒外に参考表示するか、非表示 — 実装時は参考ラベルのみ推奨）
- 使用率・通知も裁量 ÷ 予算

**予算無効時**

- 予算枠のない横向き積み上げ棒（幅 = 支出合計 100%、必須 + 裁量）
- 「予算を設定すると、予算に対する進捗が見えます」等の短い導線（設定へ）

**スコープ外**: チャートライブラリ導入（Compose `Row` + `Box` + `weight` で自前）

**オンボーディングフロー（合意）**

```
① ようこそ → ② APIキー → ③ 月次予算（新規） → ④ カメラ → ⑤ 通知 → ⑥ 完了
```

- ③ スキップ → `budgetEnabled = false`。後から設定で有効化
- ⑤ 許可 → **解析失敗のみ ON**。拒否 → すべて OFF

**スコープ外**: ジャンル別予算、通知日の自由設定、AI 節約アドバイス、チャートライブラリ導入（Compose 自前で十分）

### 柱 4（任意・余力があれば）

- B1 店名・商品名検索
- A2 分析深化（店舗・カテゴリ別の **表示のみ**。アドバイスなし）

---

## 3. ブレスト候補（未評価・参照用）

### A. 洞察・行動につなげる

| ID | アイデア | メモ |
|----|----------|------|
| A1 | 裁量支出の月次目安 + 閾値アラート | 要件 §7 将来項目。Phase 9 と直結 |
| A2 | 分析深化（カテゴリ・店舗別の裁量内訳） | 新 API 不要 |
| A3 | 前月比・推移（裁量 ○○円 → △△円） | ダッシュボード拡張 |
| A4 | 「今週の裁量」ウィジェット / 通知 | ~~コーチ寄り~~ → 月次全体予算通知に統合 |
| A5 | 固定費・サブスク検出一覧 | 裁量と補完関係 |

### B. 日常の使いやすさ

| ID | アイデア | メモ |
|----|----------|------|
| B1 | 店名・商品名検索 | 要件の将来項目 |
| B2 | レシート単位の再解析 | 5.1・方針変更後に有効 |
| B3 | オフライン撮影キュー | 通信弱い場面向け |
| B4 | プレビュー回転・トリミング | OCR 精度向上 |
| B5 | クイック撮影ショートカット | ランチャー / ウィジェット |

### C. Phase 9 の延長

| ID | アイデア | メモ |
|----|----------|------|
| C1 | プリセット変更時の promptBlock 自動再マージ | 小さな UX 改善 |
| C2 | 訂正例の「よく直すパターン」サマリ | コンパイル前の気づき |
| C3 | 再スコアに店名文脈を渡す | コンビニ嗜好品など |
| C4 | 境界ケースの実利用チューニング UI | 開発者向けではなく設定から？ |

### D. データ・信頼

| ID | アイデア | メモ |
|----|----------|------|
| D1 | バックアップ暗号化 | JSON を端末外に出す前提 |
| D2 | APIキー UI 改善（変更するで展開） | 計画済み |
| D3 | 解析 confidence 低い明細の一覧 | 修正優先度の可視化 |

### E. やらない／後回し（メモ）

| ID | アイデア | 理由 |
|----|----------|------|
| E1 | Drive 自動バックアップ復活 | 運用コスト。クローズ済み |
| E2 | 科目別・ジャンル別予算 | 全体予算で足りる。合意済み |
| E3 | 複数端末同期 | 個人スコープ外 |
| E4 | 全期間一括再スコア | API コスト・需要不明 |
| E5 | コーチ機能（AI アドバイス・説教） | 本人の好み。合意済み |
| E6 | テーマカラー自由選択 | ポートフォリオ向けは Material 動的色で十分（§7 参照） |
| E7 | 通知日の任意カスタム（毎週火曜など） | 月次リズム固定で足りる |

---

## 4. 採用パッケージ（合意方向）

**Phase 9.5 = 柱1（解析失敗）+ 柱2（通知整理）+ 柱3（月次全体予算）**

```
優先順:
  1. 解析失敗体験（API 上限含む）
  2. 通知設定 UI + オンボーディング連動（失敗のみデフォルト ON）
  3. 月次全体予算（オンボーディング導入 + 10/20/月末通知）
  4. （余力）検索
```

案 α の「裁量目安」は **全体予算** に置き換え。裁量だけの閾値は柱3に含めない（シンプル優先）。

---

## 5. 未決定事項

- [ ] 正式名称（Phase 9.5 / Phase 10 など）
- [x] 月次予算: オンボーディングで入力促進、スキップ時は機能無効
- [x] 集計対象: デフォルト合計、設定で裁量のみに切替
- [x] 定期通知: 10日・20日・月末（固定）
- [x] 予算分析タブ: **棒全体 = 予算**、その中に必須/裁量を積み上げ。予算無効時は支出のみ棒
- [x] 解析失敗通知: 権限許可時 **失敗のみ ON**。他は OFF。設定で個別変更 + マスタートグル
- [x] オンボーディング: 予算ステップは **APIキー直後**（③）。通知ステップ文言更新
- [x] 既存ユーザー: 予算オンボーディングは **新規のみ**（設定から後から有効化可）

---

## 6. カスタム性の方針（個人利用 × Play 公開）

### 前提

- **作り手**: 自分が使いやすいもの（個人最適）
- **公開**: ポートフォリオ + Play Store 想定 → **破綻しないデフォルト** と **説明可能な範囲** は必要
- **原則**: カスタムは **機能の意味に直結するもの** だけ。見た目・好みの細分化は最小限

### 3 層モデル（労力の切り方）

| 層 | 意味 | 例 | 方針 |
|----|------|-----|------|
| **A. 意味のある設定** | 家計の見え方・通知の中身が変わる | 予算額、集計対象（合計/裁量）、必須度方針プリセット、解析完了通知 ON/OFF | **実装する**。Play 公開でも説明しやすい |
| **B. 固定の良いデフォルト** | 好みは割れるが、選ばせなくても困らない | 予算通知日（10/20/月末）、閾値 80%/100%、Material 動的色 | **固定 or 2〜3択まで**。個別カスタム UI は作らない |
| **C. 装飾・嗜好** | 見た目・マイナーな頻度 | テーマカラー、フォント、通知の曜日指定 | **原則やらない**。ダークモードは OS 追従で足りる |

### 判断のショートカット

新しい「設定項目」を足す前に:

1. **スキップ or デフォルトで困るユーザーがいるか？** → No なら設定化しない（B 層で固定）
2. **設定を間違えると家計の数字がおかしくなるか？** → Yes ならオンボーディング or 説明文を厚く（A 層）
3. **ポートフォリオで語れるか？** → 「撮影中心の家計可視化」に関係ないなら優先度下げ（C 層）

### 今回の合意との対応

| 項目 | 層 | 決定 |
|------|-----|------|
| 月額予算・集計対象 | A | オンボーディング + 設定 |
| 予算通知 10/20/月末 | B | **固定**（設定画面に説明のみ） |
| 解析完了通知 | A | ON/OFF（**デフォルト OFF**） |
| 解析失敗通知 | A | 権限許可時のみ ON、設定で変更可 |
| 通知マスタートグル | A | すべての通知 ON/OFF |
| テーマカラー | C | **やらない**（Material3 + システムテーマ） |
| 通知頻度の自由入力 | C | **やらない** |

### Play Store / ポートフォリオ向けの言い方

- 「自分用に作ったが、**レシート撮影で家計が見える**汎用アプリとして公開」
- カスタムは **方針・予算・通知の要否** に絞り、**好みの細かいチューニングはしない** と明記できる
- スクリーンショットは **デフォルト体験** 前提で撮る（設定の組み合わせ爆発を避ける）

---

## 7. セッション記録

### 2026-07-13

#### 本人の発言要約

- もともとは家計の可視化が主目的。最近は節約もしたいが **記録メイン・節約サブ**
- コーチ寄り（機械からのアドバイス）は入れたくない
- **解析失敗（特に API 上限）** が体験としてつらい → 早めに手を入れる
- **解析完了の OS 通知は不要** → 設定で切替
- **全体予算の通知は OK**。ジャンル別予算は不要
- **月の予算 + 定期的な消費割合** がちょうどよい（当初は N 日ごと案）

#### 解釈メモ

- 「節約」は Phase 9 の必須度方針 + 全体予算の **数字の鏡** で足りる
- 解析失敗は **UI への常時露出と再送信** が本丸

### 2026-07-14

#### 本人の発言要約

- **予算**: オンボーディングで紹介 + 入力促進。スキップ可 → **スキップ時は機能無効**
- **集計**: デフォルトは支出合計。設定で **裁量のみ** に切替可能に
- **定期通知**: 月次に合わせ **10日・20日・月末**（N 日ごと案は不採用）
- **カスタム性**: 個人利用だが Play 公開予定。**テーマ色や通知頻度などにどこまで労力を割くか** を検討したい

#### 解釈メモ

- 予算は **オンボーディングで一度決める** 体験が自然（APIキーと同型）
- 通知日は **3 点固定** で実装コストと説明コストが最小
- カスタムは §6 の 3 層モデルで線引き（A のみ積極、B は固定、C は捨てる）

### 2026-07-14（続き）

#### 本人の発言要約

- **予算オンボーディング**: **新規インストールのみ**で十分
- **分析タブ**: **横向きの必須/裁量積み上げ棒グラフ** のイメージ

#### 解釈メモ

- 既存ユーザーは `OnboardingPrefs` 完了済み → 予算ステップは初回 Wizard にだけ追加。有効化は設定タブから
- 分析 UI は現行の単色 `LinearProgressIndicator` を置換。予算は別行のテキスト + 使用率で足す（コーチ文なし）

### 2026-07-14（続き2）

#### 本人の発言要約

- 予算ステップ: **APIキー直後**で OK
- 通知権限をもらったとき → **失敗通知だけ ON**
- 設定で種別ごとに変更可能 + **すべての通知** マスタートグル

#### 解釈メモ

- 完了・要確認・予算通知は初期 OFF。失敗だけ「権限を活かす」用途に限定
- マスター OFF は送信パスの最上流でガード（個別 ON でも出さない）

### 2026-07-14（続き3）

#### 本人の発言要約

- 分析タブ: **グラフ全体を予算**とし、その中で必須・裁量を積み上げる（予算枠が基準）

#### 解釈メモ

- 以前案（支出 100% の中で必須/裁量分割）から変更。予算が「容器」、支出は充填量
- 予算無効時のみ支出ベースのフォールバック棒を表示
